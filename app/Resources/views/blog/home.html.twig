{# app/Resources/views/blog/home.html.twig #}

{% extends 'base.html.twig' %}

{% block body %}
    {% for item in posts %}
        <h1> {{ item.author }}</h1>
        <p>{{item.content}}</p>
    {% endfor %}
(#
    {{ form_start(form) }}
    {{ form_widget(form) }}
    {{ form_end(form) }}
#}

{% endblock  %}
